Keeps this empty directory in git. Do not delete — evidence is filed here as
artifacts/YYYY/MM/, and the directory must exist before the first source
arrives. Remove this file once real evidence is filed.
